asgiref==3.11.1
dj-database-url==3.1.2
Django==6.0.3
django-cors-headers==4.9.0
django-extensions==4.1
djangorestframework==3.17.0
# gunicorn: servidor WSGI de produccion (Procfile) — manage.py runserver
# no es apto para producción.
gunicorn==26.1.0
# lxml + xmlsec: validacion de firma XAdES de comprobantes SUNAT
# (apps/invoicing/services_validacion.py, sesion 59). xmlsec requiere
# libxmlsec1 (dependencia de SISTEMA, no solo de Python) — ver la nota
# sobre Railway/nixpacks en CLAUDE.md antes de desplegar esto.
lxml==6.1.1
openpyxl==3.1.5
pillow==12.1.1
# pypdf: validacion estructural de PDF cargados (Factura.pdf_file y
# documentos de retencion/detraccion, apps/invoicing/services_archivos.py)
# — confirma que el archivo parsea como documento completo, no solo que
# empiece con la firma %PDF-. Puro Python, sin dependencia de sistema.
pypdf==6.14.2
psycopg2-binary==2.9.11
python-dotenv==1.2.2
# requests: import directo en apps/base/{services_correo,graph_auth,utils}.py
# (integracion Microsoft Graph / OneDrive, desde la sesion 73). Antes venia
# transitivo de pyHanko<0.37 (dep de xhtml2pdf); pyHanko 0.37 lo reemplazo
# por aiohttp, asi que hay que declararlo aca de forma explicita.
requests==2.32.5
sqlparse==0.5.5
tzdata==2025.3
whitenoise==6.12.0
xhtml2pdf==0.2.17
xmlsec==1.3.17
